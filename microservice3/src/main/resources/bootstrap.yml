# Servidor HTTP
server:
  port: 8080

# Propriedades do Spring
spring:
  application:
    name: poc-ms3

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
    healthcheck:
      enabled: true
  instance:
    leaseRenewalIntervalInSeconds: 5
